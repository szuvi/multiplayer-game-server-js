# Database Configuration (RDS Simulation)
# For local development without Docker, use localhost
# For Docker EC2 simulation, use the postgres container name
DATABASE_URL="postgresql://gameserver:gameserver_password@multiplayer-rds-postgres:5432/multiplayer_game?schema=public"

# Redis Configuration (EC2 Simulation)
# Use 'redis' when running in docker-compose.ec2.yml
# Use 'localhost' for local development
REDIS_HOST=redis
REDIS_PORT=6379

# Node Environment
NODE_ENV=production

